Hermit is easiest to understand once it is running locally.

The safe default path is simple: install the required tools, clone the repo, add your OpenAI key to Keychain, then start the three main processes:

- `npm start`
- `npm run heartbeat-daemon`
- `npm run explorer`

From there, the first conversation usually creates the first focused role and starts shaping the workspace around the job you describe.
